/// Smart Waste Sorting — Scan Provider
/// State management untuk proses scan dan riwayat.
/// Akan diimplementasikan di Stage 3.
library;

import 'package:flutter/material.dart';

/// Placeholder — akan diimplementasikan di Stage 3.
class ScanProvider extends ChangeNotifier {
  // State: isLoading, currentResult, scanHistory, error
  // Methods: scanImage(), loadHistory(), deleteResult(), getStats()
}
