/// Smart Waste Sorting — History Screen
/// Riwayat semua scan dengan search, filter, dan delete.
/// Akan diimplementasikan di Stage 6.
library;

import 'package:flutter/material.dart';

/// Placeholder — akan diimplementasikan di Stage 6.
class HistoryScreen extends StatelessWidget {
  const HistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('History Screen — Stage 6')),
    );
  }
}
