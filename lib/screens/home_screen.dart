/// Smart Waste Sorting — Home Screen
/// Dashboard utama dengan stats, kategori, dan recent scans.
/// Akan diimplementasikan di Stage 5.
library;

import 'package:flutter/material.dart';

/// Placeholder — akan diimplementasikan di Stage 5.
class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Home Screen — Stage 5')),
    );
  }
}
