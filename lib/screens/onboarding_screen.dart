/// Smart Waste Sorting — Onboarding Screen
/// Multi-page onboarding edukatif tentang klasifikasi sampah.
/// Akan diimplementasikan di Stage 4.
library;

import 'package:flutter/material.dart';

/// Placeholder — akan diimplementasikan di Stage 4.
class OnboardingScreen extends StatelessWidget {
  const OnboardingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Onboarding Screen — Stage 4')),
    );
  }
}
