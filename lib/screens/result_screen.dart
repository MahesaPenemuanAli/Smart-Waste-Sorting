/// Smart Waste Sorting — Result Screen
/// Tampilan hasil klasifikasi AI: kategori, confidence, tips.
/// Akan diimplementasikan di Stage 6.
library;

import 'package:flutter/material.dart';

/// Placeholder — akan diimplementasikan di Stage 6.
class ResultScreen extends StatelessWidget {
  const ResultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Result Screen — Stage 6')),
    );
  }
}
