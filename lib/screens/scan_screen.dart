/// Smart Waste Sorting — Scan Screen
/// Fitur utama: upload gambar dari kamera/galeri untuk klasifikasi.
/// Akan diimplementasikan di Stage 5.
library;

import 'package:flutter/material.dart';

/// Placeholder — akan diimplementasikan di Stage 5.
class ScanScreen extends StatelessWidget {
  const ScanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Scan Screen — Stage 5')),
    );
  }
}
