/// Smart Waste Sorting — Splash Screen
/// Animated splash screen dengan Lottie animation.
/// Akan diimplementasikan di Stage 4.
library;

import 'package:flutter/material.dart';

/// Placeholder — akan diimplementasikan di Stage 4.
class SplashScreen extends StatelessWidget {
  const SplashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Splash Screen — Stage 4')),
    );
  }
}
