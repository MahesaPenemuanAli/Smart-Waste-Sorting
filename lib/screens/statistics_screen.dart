/// Smart Waste Sorting — Statistics Screen
/// Statistik penggunaan: pie chart, bar chart, eco score.
/// Akan diimplementasikan di Stage 7.
library;

import 'package:flutter/material.dart';

/// Placeholder — akan diimplementasikan di Stage 7.
class StatisticsScreen extends StatelessWidget {
  const StatisticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Statistics Screen — Stage 7')),
    );
  }
}
